function say(t,ok){let m=document.getElementById('msg');m.textContent=t;m.className='msg '+(ok?'ok':'err');}async function doReset(e){e.preventDefault();let token=new URLSearchParams(location.search).get('token');let r=await fetch('/auth/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password:document.getElementById('password').value})});if(r.status===200){say('Password set — sign in with it now.',true);setTimeout(()=>location.href='/login',900);}else{say('That link is invalid or expired — request a new one from the sign-in page.');}return false;}