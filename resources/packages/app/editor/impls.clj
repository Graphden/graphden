(ns graphden.packages.app.editor.impls
  "Graph editor package — no base-fn impls. CSS/JS loading is a
   fn-def composition over `:read-resource` / `:concat-resources`.
   This file stays to keep the package's impls.clj convention.")


(def impls {})
