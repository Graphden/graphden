{:name "core"
 :version "1.0.0"
 :description "Core primitives: arithmetic, comparison, logic, HOF, collections, strings, system, refinements"
 :dependencies []
 ;; refinements load FIRST so their type aliases are registered
 ;; before collections / strings / system / etc. try to resolve
 ;; `:type :non-negative-int` etc. in their fns.edn declarations.
 :modules ["refinements" "arithmetic" "logic" "hof" "collections" "strings" "system" "concurrency" "variants"]}
